<%= form_for(@search_info) do |f| %>
  <% if @search_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@search_info.errors.count, "error") %> prohibited this search_info from being saved:</h2>

      <ul>
      <% @search_info.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
